import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  spacing: undefined, 
  palette: {
    primary: {
      main: "#04091c",
    },
    secondary: {
      main: "#7a26ef",
    },
  },
});

export default theme
